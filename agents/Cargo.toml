[package]
name = "neumannarch-agents"
version.workspace = true
edition.workspace = true
description = "The agent frontend of Neumannarch: scripted opponents and the harness"

[dependencies]
neumannarch-protocol = { path = "../protocol" }
neumannarch-sim = { path = "../sim" }

[lints]
workspace = true
