[package]
name = "neumannarch-protocol"
version.workspace = true
edition.workspace = true
description = "Every value two machines of Neumannarch exchange"

[dependencies]
ciborium = "0.2.2"
neumannarch-sim = { path = "../sim" }
serde = { version = "1.0.229", features = ["derive"] }

[lints]
workspace = true
