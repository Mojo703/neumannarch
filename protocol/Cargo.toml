[package]
name = "probe-protocol"
version.workspace = true
edition.workspace = true
description = "Every value two machines of Probe Game exchange"

[dependencies]
ciborium = "0.2.2"
probe-sim = { path = "../sim" }
serde = { version = "1.0.229", features = ["derive"] }

[lints]
workspace = true
