[package]
name = "probe-sim"
version.workspace = true
edition.workspace = true
description = "The deterministic lockstep simulation of Probe Game"

[dependencies]
libm = "0.2"

[lints]
workspace = true
