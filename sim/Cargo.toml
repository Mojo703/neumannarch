[package]
name = "neumannarch-sim"
version.workspace = true
edition.workspace = true
description = "The deterministic lockstep simulation of Neumannarch"

[dependencies]
libm = "0.2"
serde = { version = "1.0.229", features = ["derive"] }

[lints]
workspace = true
